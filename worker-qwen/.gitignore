.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea

# 실제 접속 정보가 든 env 는 올리지 않는다. 형식만 *.example 로 공유한다.
.env
.env.docker
!*.example

# Hugging Face 가중치 캐시 (수 GB)
model_cache/

# 보이스 클로닝 참조는 음성 데이터라 저장소에 올리지 않는다. README 는 유지.
custom_voices/*
!custom_voices/README.md
