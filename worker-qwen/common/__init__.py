"""워커 공통 모듈.

**이 패키지의 파일은 모든 TTS 워커 저장소가 똑같이 들고 있다.**
`worker-supertonic/common/` 과 `worker-qwen/common/`
의 내용은 글자 하나까지 같아야 한다.

여기를 고칠 때는 **모든 워커 저장소를 함께 고친다.** 한쪽만 고치면 두 워커가 같은 문장을
다르게 읽거나 다른 오디오를 내놓는데, 예외도 로그도 없이 조용히 갈라지므로 알아채기 어렵다.

저장소를 나눠 두고 복제하는 이유는 워커가 아직 둘뿐이고, 공용 패키지 저장소를 따로 만들고
운영하는 비용이 얻는 것보다 크기 때문이다. **세 번째 워커가 생기면 그 계산이 바뀐다** —
그때는 별도 패키지로 빼는 것을 다시 검토할 것.

자세한 배경은 `common/README.md` 참고.
"""
